export const lightTheme = `
  color: var(--dark);
  background: var(--light);
  transition: color 0.15s ease-out, background 0.25s ease-in;
`;

export const darkTheme = `
  color: var(--light);
  background: var(--dark);
  transition: color 0.15s ease-out, background 0.25s ease-in;
`;
